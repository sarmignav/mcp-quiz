[project]
name = "mcp-quiz"
version = "0.1.0"
description = "MCP Quiz server"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=2.13.1",
    "python-dotenv>=1.2.1",
    "supabase>=2.24.0",
]
