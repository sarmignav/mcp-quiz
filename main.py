from mcp_quiz.server import server

if __name__ == "__main__":
    # This will depend on how fastmcp expects to run the server
    # You might need to adapt this based on fastmcp's requirements
    server.run()
